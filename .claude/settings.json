{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'uv run'; then echo 'Reminder: This project uses `just` recipes (e.g. `just test`, `just build`, `just check`). Consider using a just recipe instead of running uv directly.'; fi"
          }
        ]
      }
    ]
  }
}
